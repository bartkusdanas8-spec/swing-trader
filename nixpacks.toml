[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
  "python3.11 -m venv /app/.venv311",
  "/app/.venv311/bin/pip install --upgrade pip setuptools wheel",
  "/app/.venv311/bin/pip install -r requirements.txt"
]

[start]
cmd = "/app/.venv311/bin/python bot.py"
